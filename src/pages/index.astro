---
import BaseLayout from '../layouts/BaseLayout.astro';
import { SITE } from '../data/site';
import { REVIEWS, GOOGLE_RATING, REVIEWS_URL } from '../data/reviews';
import { FEATURED_TESTIMONIALS } from '../data/videos';
import VideoTestimonial from '../components/VideoTestimonial.astro';
import PressStrip from '../components/PressStrip.astro';

const problems = [
  {
    title: 'The rehab ran out before you were back',
    body: "Twelve sessions, a discharge note, and a body that still can't do the thing you actually miss. <strong>Nobody in that chain was paid to take you the rest of the way.</strong> That distance is where this work lives.",
  },
  {
    title: 'The relief never lasted',
    body: "PT, chiropractic, injections, a new mattress. Each one turned the volume down for a couple of weeks. <strong>You don't need more relief — you need to stop needing it.</strong>",
  },
  {
    title: 'Cleared, and still not trusting it',
    body: "Your surgeon says the joint is fine. Your body has not agreed to that yet. <strong>You don't talk yourself out of that. You out-train it</strong> — one graded step at a time.",
  },
];

const byArea = [
  {
    title: 'Back Pain & Sciatica',
    slug: '/what-we-treat/chronic-back-pain',
    body: "Three months or more, and PT, adjustments and injections have all worn off. The tissue stopped being the story a while ago — this is the approach that treats what replaced it.",
    also: { label: 'Pain running down your leg? Sciatica →', slug: '/what-we-treat/sciatica' },
  },
  {
    title: 'Neck Pain',
    slug: '/what-we-treat/neck-pain',
    body: 'Stiffness and headaches that come back no matter how the desk is set up. It is probably not your posture alone — and a neck with real strength behind it handles a long day.',
  },
  {
    title: 'Shoulder Pain',
    slug: '/what-we-treat/shoulder-pain',
    body: "You can't sleep on that side, and the rest and the cortisone both wore off. Shoulders don't come back from being protected. They come back from being loaded well.",
  },
  {
    title: 'Knee Pain',
    slug: '/what-we-treat/knee-pain',
    body: 'Stairs, the descent on a hike, the word "degenerative" on a scan. "Bone on bone" describes an X-ray. It is not a prognosis and not a reason to stop loading the leg.',
  },
];

const bySituation = [
  {
    title: 'Post-Surgical Return-to-Sport',
    slug: '/what-we-treat/post-surgical-return-to-sport',
    body: "Someone wrote \"functional\" on a form and the referral ran out. You didn't have surgery to be functional. This is the six-to-nine month rebuild nobody was paid to give you.",
  },
  {
    title: 'Masters Athlete Injuries',
    slug: '/what-we-treat/masters-athlete-injuries',
    body: 'Runners, cyclists, skiers and lifters over 40. Train, break, rest, repeat — and each lap costs more than the last. The injuries are not the problem. The cycle is.',
  },
];

const differentiators = [
  'One-on-one, every session. Never a class, never shared with three other people',
  '60–90 minutes, because that is genuinely what the work takes',
  'Built around your sport and your week — not a protocol printed for everybody',
  'Your strength coach and your therapist are the same person. No handoffs, no contradicting advice',
  'Thirty years of coaching, plus the pain science most providers were never taught',
  'A number of sessions, and a graduation. Not an open tab',
];
---

<BaseLayout
  title="Denver Pain Relief & Return-to-Sport Rehab | Jamie Atlas"
  description={SITE.description}
>

  <!-- HERO -->
  <section class="px-6 pt-16 pb-20 md:pt-28 md:pb-28">
    <div class="max-w-4xl mx-auto text-center">
      <h1 class="mb-6">
        Get out of chronic pain.<br />
        Get back to your sport.
      </h1>
      <p class="text-xl md:text-2xl text-[var(--color-text-muted)] max-w-2xl mx-auto mb-10 leading-snug">
        Cognitive Functional Therapy + 30 years of strength coaching, for Denver adults 40+ who refuse to slow down.
      </p>
      <a href="/book" class="cta-button text-lg">
        Book a 30-minute consultation
      </a>
    </div>

    <div class="mt-14">
      <PressStrip compact={true} />
    </div>

    <div class="max-w-5xl mx-auto mt-16 px-0">
      <img
        src="/studio-coaching.jpg"
        alt="Jamie Atlas coaching two clients through a movement assessment at his downtown Denver studio"
        width="1400"
        height="718"
        fetchpriority="high"
        class="rounded-2xl border border-[var(--color-border)] w-full"
      />
    </div>
  </section>

  <!-- THE PROBLEM -->
  <section class="px-6 py-20 bg-[var(--color-surface)] border-y border-[var(--color-border)]">
    <div class="max-w-6xl mx-auto">
      <h2 class="text-center mb-6">If any of this sounds familiar, you're in the right place</h2>
      <p class="text-center text-lg text-[var(--color-text-muted)] max-w-2xl mx-auto mb-14 leading-relaxed">
        Most people who find this studio have already been somewhere else. Often several somewheres.
        <strong class="text-[var(--color-text)]">We get it.</strong>
      </p>
      <div class="grid md:grid-cols-3 gap-10 md:gap-12">
        {problems.map(p => (
          <div>
            <h3 class="mb-4">{p.title}</h3>
            <p class="text-[var(--color-text-muted)] leading-relaxed" set:html={p.body} />
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- THE LANCET CREDIBILITY HAMMER -->
  <section class="px-6 py-24">
    <div class="max-w-3xl mx-auto text-center">
      <p class="font-serif italic text-[var(--color-primary)] mb-3 text-lg">The evidence</p>
      <h2 class="mb-8">Backed by science most providers were never taught</h2>
      <p class="text-lg md:text-xl text-[var(--color-text-muted)] mb-6 leading-relaxed">
        <strong class="text-[var(--color-text)]">Plenty of things help chronic pain for six weeks.</strong> Very little is still ahead of usual care three years later.
        Cognitive Functional Therapy is. In the <strong class="text-[var(--color-text)]">2023 Lancet RESTORE trial</strong> — and again at the three-year follow-up published in 2025 — it produced large, durable improvements in pain and disability.
      </p>
      <p class="text-lg md:text-xl text-[var(--color-text-muted)] leading-relaxed">
        Then I add the part most CFT practitioners can't: three decades of strength coaching, so the change has something to stand on.
        Calming a pain system without rebuilding capacity is temporary. Rebuilding capacity without calming it never gets started.
      </p>
    </div>
  </section>

  <!-- PROOF: VIDEO TESTIMONIALS -->
  <section class="px-6 py-24 bg-[var(--color-surface)] border-y border-[var(--color-border)]">
    <div class="max-w-5xl mx-auto">
      <div class="max-w-3xl mx-auto text-center mb-14">
        <p class="font-serif italic text-[var(--color-primary)] mb-3 text-lg">What that looks like</p>
        <h2 class="mb-6">Watch pain change in real time</h2>
        <p class="text-lg text-[var(--color-text-muted)] leading-relaxed">
          Not cures, and not typical of every case. When pain is driven by a protective nervous system rather
          than damaged tissue, it turns out to be changeable — and that's where the work starts.
        </p>
      </div>

      <div class="grid gap-14">
        {FEATURED_TESTIMONIALS.map(v => <VideoTestimonial video={v} />)}
      </div>

      <p class="text-center mt-12">
        <a href="/testimonials" class="font-semibold">See more client results →</a>
      </p>
    </div>
  </section>

  <!-- WHAT WE TREAT -->
  <section class="px-6 py-24 bg-[var(--color-surface)] border-y border-[var(--color-border)]">
    <div class="max-w-6xl mx-auto">
      <h2 class="text-center mb-4">Start here if you're in pain</h2>
      <p class="text-center text-[var(--color-text-muted)] max-w-2xl mx-auto mb-14">
        Pick the part that's stopping you. Same method behind all four — what changes is what we find driving it.
      </p>
      <div class="grid md:grid-cols-2 gap-6">
        {byArea.map(c => (
          <div class="p-7 border border-[var(--color-border)] rounded-xl bg-[var(--color-background)] hover:border-[var(--color-primary)] transition-all">
            <h3 class="mb-3">
              <a href={c.slug} class="text-[var(--color-primary)] no-underline">{c.title}</a>
            </h3>
            <p class="text-[var(--color-text-muted)] text-base leading-relaxed">{c.body}</p>
            {c.also && (
              <p class="mt-4 text-sm"><a href={c.also.slug}>{c.also.label}</a></p>
            )}
          </div>
        ))}
      </div>

      <h2 class="text-center mt-24 mb-4">Start here if your doctor already cleared you</h2>
      <p class="text-center text-[var(--color-text-muted)] max-w-2xl mx-auto mb-14">
        Signed off, told you're fine, and still nowhere near the bike, the slopes or the barbell.
      </p>
      <div class="grid md:grid-cols-2 gap-6">
        {bySituation.map(c => (
          <a
            href={c.slug}
            class="block p-7 border border-[var(--color-border)] rounded-xl bg-[var(--color-background)] hover:border-[var(--color-primary)] hover:shadow-sm transition-all no-underline"
          >
            <h4 class="mb-3 text-[var(--color-primary)]">{c.title}</h4>
            <p class="text-[var(--color-text-muted)] text-base leading-relaxed">{c.body}</p>
          </a>
        ))}
      </div>

      <p class="text-center mt-16 text-lg">
        Both routes run on the same method —
        <a href="/what-we-treat/cognitive-functional-therapy">Cognitive Functional Therapy</a>,
        plus thirty years of strength coaching.
      </p>
    </div>
  </section>

  <!-- HOW IT'S DIFFERENT -->
  <section class="px-6 py-24">
    <div class="max-w-3xl mx-auto">
      <h2 class="text-center mb-14">How this is different from what you've tried</h2>
      <ul class="space-y-5">
        {differentiators.map(d => (
          <li class="flex items-start gap-4">
            <span class="text-[var(--color-primary)] text-2xl leading-none mt-1 flex-shrink-0">✓</span>
            <span class="text-lg leading-snug">{d}</span>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <!-- REVIEWS -->
  <section class="px-6 py-24 bg-[var(--color-surface)] border-y border-[var(--color-border)]">
    <div class="max-w-4xl mx-auto">
      <h2 class="text-center mb-4">Rated {GOOGLE_RATING.stars.toFixed(1)} on Google</h2>
      <p class="text-center text-[var(--color-text-muted)] mb-14">Verbatim, verifiable, unpaid.</p>
      <div class="grid md:grid-cols-3 gap-8">
        {REVIEWS.slice(0, 3).map(r => (
          <blockquote class="border-l-4 border-[var(--color-primary)] pl-5">
            <p class="leading-relaxed mb-3">"{r.text}"</p>
            <footer class="text-sm text-[var(--color-text-muted)]">— {r.name}</footer>
          </blockquote>
        ))}
      </div>
      <p class="text-center mt-12">
        <a href="/reviews" class="font-semibold">More client reviews →</a>
        <span class="text-[var(--color-text-muted)] px-2">·</span>
        <a href={REVIEWS_URL}>See all {GOOGLE_RATING.count} on Google</a>
      </p>
    </div>
  </section>

  <!-- FINAL CTA -->
  <section class="px-6 py-24 bg-[var(--color-primary)] text-on-dark">
    <div class="max-w-3xl mx-auto text-center">
      <h2 class="mb-6 text-white">Stop waiting for the pain to go away on its own</h2>
      <p class="text-xl mb-10 opacity-95">
        A 30-minute consultation tells you whether this is the right fit. No pressure, no insurance forms.
      </p>
      <a href="/book" class="cta-button cta-button-accent text-lg">
        Book a consultation
      </a>
      <p class="mt-8 text-sm opacity-80">
        Or call <a href={`tel:${SITE.contact.phoneTel}`} class="text-white underline">{SITE.contact.phone}</a>
      </p>
    </div>
  </section>

</BaseLayout>
